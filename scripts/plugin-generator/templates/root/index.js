import { content as packageJson } from './package.json.js';
import { content as podspec } from './aide-browser.podspec.js';

export const rootFiles = {
  'package.json': packageJson,
  'AideBrowser.podspec': podspec,
};