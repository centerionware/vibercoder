import React from 'react';

// This component is no longer used as the browser functionality has been
// refactored into a custom native Capacitor plugin. This file is kept
// to prevent build errors from lingering file references in some environments.
const BrowserOverlay: React.FC = () => {
  return null;
};

export default BrowserOverlay;
